{
  "_reason": "cc-scan is a single-file, zero-dependency CLI: print() is its legitimate output, and the thresholds (120/40/3) must sit in the DEFAULTS table so that copying the file is enough to run it. This is an exception WITH A STATED REASON, not a rule switched off to make the number go green.",
  "skipRules": ["DEBUG_STATEMENT", "MAGIC_NUMBER"],
  "ignoreDirs": ["fixtures", "demo"],
  "minTestRatio": 0.0
}
